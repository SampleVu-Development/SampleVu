/*
  TODO: Add save button, where upon press, saves survey to database via API
  Validates no questions are currently being created (i.e., all questions
    are non-empty (multiple choice should have answers
  API to be added later
 */

const SaveSurveyButton = () => {
  return <button>Save</button>
}

export default SaveSurveyButton
