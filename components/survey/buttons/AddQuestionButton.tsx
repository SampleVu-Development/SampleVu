/*
  TODO: Add question button, where upon press, pushes new empty card at bottom
  Default TBD (set type)
 */

const AddQuestionButton = () => {
  return <button>Add Question</button>
}

export default AddQuestionButton
